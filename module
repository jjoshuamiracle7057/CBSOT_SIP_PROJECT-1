import joblib
from pathlib import Path

# Create the models directory
Path("models").mkdir(parents=True, exist_ok=True)

# Save the models inside the models/ folder
joblib.dump(rf_model, "models/random_forest_model.pkl")      # Your trained Random Forest model
joblib.dump(preprocessor, "models/preprocessor.pkl")          # Your data preprocessing pipeline
joblib.dump(kmeans_model, "models/kmeans_model.pkl")          # Your K-Means clustering model

print("Models successfully saved to the models/ folder!")
